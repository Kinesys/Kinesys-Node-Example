npm을 통하여 외부 모듈을 설치할 수 있고 require를 통하여 외부 모듈을 사용할 수 있다.

npm을 설치하면 package.json에 json파일로 저장된다. 

웹 브라우저는 클라이언트이고 클라이언트에서는 어떤 동작을 할 때하다 서버로 요청을 하며 그에따른 응답을 해야한다. 
-응답을 하기 위해서는 라우터가 필요하며 소스코드상의 app.get부분은 하나의 라우터 역할을 한다.

/를 라우터라고 하고 라우터 뒤의 링크를 지정해줄 수 있다.